fastapi==0.115.6
uvicorn[standard]==0.32.1
httpx==0.28.1
python-multipart==0.0.20
supabase>=2.10.0,<3
# MinerU document parser (CLI `mineru`). Installed in Docker; local dev without MinerU still runs the API but extraction fails honestly at runtime.
mineru[all]>=3.0.0
