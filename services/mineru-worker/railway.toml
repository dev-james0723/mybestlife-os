# Deploy this folder as the Railway service root (Dashboard → Service → Settings → Root Directory: services/mineru-worker).
# https://docs.railway.com/guides/dockerfiles

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
