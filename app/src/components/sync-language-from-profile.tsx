"use client";

/**
 * UI language is driven by the `[locale]` URL segment (`LocaleShell`). Profile
 * language is updated when the user saves Settings or uses the top bar language
 * switcher so the next session matches their choice.
 */
export function SyncLanguageFromProfile() {
  return null;
}
