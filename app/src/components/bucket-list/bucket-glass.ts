export {
  osControlSizeClassName as bucketControlSize,
  osDialogSurfaceClassName as bucketDialogSurface,
  osFrostedPanelClassName as bucketFrostedPanel,
  osGlassControlClassName as bucketGlassControl,
  osGlassPanelClassName as bucketGlassPanel,
  osIconControlSizeClassName as bucketIconControlSize,
  osMapSurfaceClassName as bucketMapSurface,
  osPrimaryControlClassName as bucketPrimaryControl,
  osSegmentedShellClassName as bucketSegmentedShell,
  osSheenClassName as bucketSheen,
  osSheetSurfaceClassName as bucketSheetSurface,
  osSolidPanelClassName as bucketSolidPanel,
} from "@/components/ui/os-glass";
